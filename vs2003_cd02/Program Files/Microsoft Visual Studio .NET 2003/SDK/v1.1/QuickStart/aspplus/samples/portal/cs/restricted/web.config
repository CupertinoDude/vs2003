<configuration>
  <system.web>
        <authorization>
            <deny users="?" />
            <allow users="*" /> <!-- Allow all (other) users -->
        </authorization>
  </system.web>
</configuration>